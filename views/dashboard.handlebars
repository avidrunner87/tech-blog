<header class="no-padding">
	{{> dashboardNav }}
</header>
<main class="container">
    <h4>Dashboard</h4>
    <div class="section">
        {{#each posts as | post|}}
            {{> postUser-details}}
        {{/each}}
    </div>

    {{> postUser }}
    {{> commentPost }}
</main>
