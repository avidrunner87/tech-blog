<header class="no-padding">
	{{> mainNav }}
</header>
<main class="container">
    <div class="section">
        {{#each posts as | post|}}
            {{> post-details}}
        {{/each}}
    </div>

    {{> commentPost }}
</main>
